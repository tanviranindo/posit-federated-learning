# Posit-Enhanced Federated Learning Dependencies

# Core machine learning
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Posit arithmetic - the key innovation
# Note: SoftPosit can be tricky to install. See installation guide for help.
# If it fails, the code will fall back to high-precision floating-point.
# softposit

# Federated learning and statistics
scikit-learn>=1.3.0
scipy>=1.10.0

# System monitoring (for measuring energy usage on different architectures)
psutil>=5.9.0

# Docker for cross-platform deployment
docker>=6.0.0

# Visualization (for seeing the difference in training stability)
matplotlib>=3.7.0

# Configuration and utilities
pyyaml>=6.0
tqdm>=4.65.0

# Optional: Advanced experiment tracking
# wandb  # Uncomment if you want experiment logging